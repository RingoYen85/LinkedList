nt maxIndex(const IntArray & array) {
  if (array.size() == 0) {
    return -1;
  }
  int answer = 0;
  for (int i = 1; i < array.size(); i++) {
    if (array[i] > array[answer]) {
      answer = i;
    }
  }
  return answ